int fun(int x, int y) {
  float z;
  z = x * y / 1.523;
  return z;
}
float opa(string z, int x) {
  float y;
  int aopa;
  if (z > x) {
    y = x * 39.42;
  } else {
    y = x * 53.234;
  }
  return y;
}
{
  int x, y;
  int f, g;
  float aferss;
  string z;
  z = "abc";
  g = fun(x, y);
  f = x * y / fun(g, x);
  if (f > 2) {
    f = f + 1;
  } else {
    while (y >= 0) {
      f = opa(z, f * 3);
      y = y - 1;
    }
  }
  affers = (f + g / y) * 34 - x * f;
  print(affers);
  read(x);
}